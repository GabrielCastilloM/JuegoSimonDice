var nombre = 'Gabriel', apellido = 'Castillo'

var nombreEnMayusculas = nombre.toUpperCase()
var nombreEnMinusculas = nombre.toLowerCase()

var primeraLetraDelNombre = nombre.charAt(0)
var cantidadDeLetrasDelNombre = nombre.length

var nombreCompleto = `${nombre} ${apellido.toUpperCase()}`

var str = nombre.substr(1,2)//para traer carecteres segun su posición

var ultimaLetraDelNombre = nombre.substr(-1) //para mostar ultima letra length y since tambien sirven